{
  "name": "@creixement/cloud-runtime",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/src/*.test.js"
  },
  "dependencies": {
    "cron-parser": "^5.3.1"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "@vercel/node": "^5.3.18",
    "typescript": "^5.9.2"
  }
}